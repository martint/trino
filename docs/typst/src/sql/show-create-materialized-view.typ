#import "/lib/trino-docs.typ": *

#anchor("doc-sql-show-create-materialized-view")
= SHOW CREATE MATERIALIZED VIEW

== Synopsis

#code-block("text", "SHOW CREATE MATERIALIZED VIEW view_name")

== Description

Show the SQL statement that creates the specified materialized view #raw("view_name").

== See also

- create-materialized-view
- drop-materialized-view
- refresh-materialized-view
